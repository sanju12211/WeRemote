<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.consumerir"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeRemote">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.WeRemote.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity" android:exported="false" />
        <activity android:name=".ui.DeviceTypeActivity" android:exported="false" />
        <activity android:name=".ui.BrandActivity" android:exported="false" />
        <activity android:name=".ui.MatchActivity" android:exported="false" />
        <activity android:name=".ui.RemoteActivity" android:exported="false" />
        <activity android:name=".ui.AcMatchActivity" android:exported="false" />
        <activity android:name=".ui.AcRemoteActivity" android:exported="false" />

    </application>

</manifest>
